# Scripts

Use these scripts to pull models and to bootstrap the environment.

- pull_models.ps1 : Uses Ollama CLI to pull recommended models.
- install_env.ps1 : Creates a Python venv and installs packages.
- run_server.ps1 : Starts the FastAPI server with environment variables.
